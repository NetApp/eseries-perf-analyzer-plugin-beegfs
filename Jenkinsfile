pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
	        echo "Nothing to do"
            }
        }
    }
    post {
        always {
            deleteDir()
        }
    }
}